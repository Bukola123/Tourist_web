const { loginUser } = require('./loginUser');
const { registerUser } = require('./registerUser');
const { saveDataAdmin,newregisterAdmin } = require('./registerAdmin');


module.exports = {
    loginUser,
    registerUser,
    saveDataAdmin,
    newregisterAdmin
};
